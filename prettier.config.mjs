/** @type {import("prettier").Config} */
const config = {
  printWidth: 100,
  semi: true,
  singleQuote: false,
  trailingComma: "all",
  arrowParens: "always",
  tabWidth: 2,
  useTabs: false,
  bracketSpacing: true,
};

export default config;
